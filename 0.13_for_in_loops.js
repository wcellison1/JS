var footballTeam = "Packers";
//for (i = 0; i < footballTeam.lenght; i++)
	for (var letter in footballTeam){
		console.log(footballTeam[letter]);
	}

	var favShow = "Lost";
//for (i = 0; i < show.lenght; i++)
	for (var letter in favShow){
		console.log(favShow[letter]);
	}

	//create a for in loop for your fav food
	// then count each vowel
	//1 need a for loop
	//2 use condition to check if letter at an index is a vowel
	//? == "a"
	var favFood = "Beer";
	for (var letter in favFood){
	
		console.log(favFood[letter]);
	}